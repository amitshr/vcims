
  <div class="well">
  <h2>Results</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Student name </th>
         <th>Test Code</th>
          <th>Marks Obtained</th>
          <th>Remarks</th>
          <th>Edit</th>
          <th>Delete</th>          
      </tr>
    </thead>
    <tbody>
    <%if @result%>
    <% @result.each do |u|%>
      <tr>
        <td><%=Student.find(u.student_id).student_name%></td>
        <td><%=Test.find(u.test_id).test_code%></td>
        <td><%=u.marks_obtained%></td>
        <td><%=u.remark%></td>
        <td><%=link_to("edit",:controller=>"results",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"results",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
