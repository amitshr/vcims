
  <div class="well">
  <h2>Courses</h2>
  <p>List of All Courses:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
      <th>Course Name</th>
      <th>Board </th>
      <th>Subject</th>
        <th>Class</th>
        <th>Fee </th>
      <th>Duration</th>
        <th>Description</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @course%>
    <% @course.each do |u|%>
      <tr>
         <td><%=u.course_name%></td>
        <td><%=Board.find(u.board_id).short_name%></td>
         <td><%=Subject.find(u.subject_id).title%></td>
         <td><%=Klass.find(u.klass_id).title%></td>
         <td><%=u.fee%></td>
         <td><%=u.duration%></td>
         <td><%=u.description%></td>
        
        <td><%=link_to("edit",:controller=>"courses",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"courses",:action=>"delete",:id=>u.id)%></td>
      
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  	<%if @error%>
		<div class="alert alert-warning">
    		<%= render"shared/error"%>
		</div>
   <%end%>

  </div>