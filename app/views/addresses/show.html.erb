

  <div class="well">
  <h2>Addresses</h2>
  <p>This table conatines the address detail about user:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>User email</th>
        <th>Address</th>
         <th>Edit</th>
          <th>Delete</th>
           <th>Check Profile</th>
      </tr>
    </thead>
    <tbody>
    <%if @address%>
    <% @address.each do |u|%>
      <tr>
        <td><%=User.find(u.user_id).email%></td>
        <td><%=u.line1+u.line2+u.city+u.country%></td>
        <td><%=link_to("edit",:controller=>"addresses",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"addresses",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
