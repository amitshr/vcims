
  <div class="well">
  <h2>Pages  </h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p><p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>CHAPTER</th>
         <th>HEADING</th>
          <th>SUB-HEAD</th>
          <th>PAGE NO.</th>
          <th>IMAGE</th> 
          <th>EDIT</th>
          <th>DELETE</th>          
      </tr>
    </thead>
    <tbody>
    <%if @page%>
    <% @page.each do |u|%>
      <tr>
        <td><%=Chapter.find(u.chapter_id).title%></td>
        <td><%=u.title%></td>
        <td><%=u.sub_title%></td>
        <td><%=u.page_number%></td>
        <td><%=u.content%></td>
        <td><%=u.image_id%></td>
        <td><%=link_to("EDIT",:controller=>"pages",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("DELETE",:controller=>"pages",:action=>"delete",:id=>u.id)%></td>
        <td><%=link_to("NEW PAGE",:controller=>"pages",:action=>"new",:id=>u.chapter_id)%></td>
        <td><%=link_to("NEW QUESTION",:controller=>"questtions",:action=>"new",:id=>u.id)%></td>
        <td><%=link_to("VIEW ALL QUESTION",:controller=>"tutorials",:action=>"pageList",:id=>u.id)%></td>
        
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>