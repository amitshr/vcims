<button type="button" class="btn btn-info" onclick= "PrintDiv()"> print
   </button>
  <div  id = "divToPrint" class="well">
  <h2>Students</h2>
  <p>All Student Details:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>User email</th>
        <th>Student Name</th>     
        <th>Contact</th>
        <th>Admission Date</th>       
         <th>Edit</th>
          <th>Delete</th>

      </tr>
    </thead>
    <tbody>
    <%if @student%>
    <% @student.each do |u|%>
      <tr>
        <td><%=User.find(u.user_id).email%></td>
        <td><%=u.student_name%></td>
        <td><%=u.mobile_number%></td>
        <td><%=u.admission_date%></td>
        <td><%=link_to("edit",:controller=>"students",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"students",:action=>"delete",:id=>u.id)%></td>
        <td><%=link_to("profile",:controller=>"students",:action=>"student",:id=>u.user_id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
