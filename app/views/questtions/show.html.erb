
  <div class="well">
  <h2>Questions</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>PAGE HEADING</th>
         <th>NO.</th>
          <th>QUESTIONS</th>
          <th>TYPE</th>
          <th>IMAGE</th>
          <th>EDIT</th>
          <th>DELETE</th>
          <th>ANSWER</th>          
      </tr>
    </thead>
    <tbody>
    <%if @question%>
    <% @question.each do |u|%>
      <tr>
        <td><%=Page.find(u.page_id).title%></td>
        <td><%=u.question_number%></td>
        <td><%=u.content%></td>
        <td><%=u.question_type%></td>
  
        <td><%=link_to("EDIT",:controller=>"questtions",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("DELETE",:controller=>"questtions",:action=>"delete",:id=>u.id)%></td>
        <td><%=link_to("NEW QUESTION",:controller=>"questtions",:action=>"new",:id=>u.page_id)%></td>
        <td><%=link_to("ANSWER",:controller=>"answers",:action=>"new",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>