
  <div class="well">
  <h2>Rooms</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Center </th>
         <th>Room Number</th>
          <th>Seats</th>
          <th>Edit</th>
          <th>Delete</th>          
      </tr>
    </thead>
    <tbody>
    <%if @room%>
    <% @room.each do |u|%>
      <tr>
        <td><%=Center.find(u.center_id).center_name%></td>
        <td><%=u.room_number%></td>
        <td><%=u.seats%></td>
        <td><%=link_to("edit",:controller=>"rooms",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"rooms",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>