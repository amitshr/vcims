
  <div class="well">
  <h2>Qualification</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Teacher</th>
         <th>Degree</th>
          <th>Board</th>
          <th>University</th>
          <th>Mode</th>
          <th>Passing Year</th> 
          <th>Marks Obatined</th>
          <th>Percentage</th>
          <th>Edit</th>
          <th>Delete</th>          
      </tr>
    </thead>
    <tbody>
    <%if @qualification%>
    <% @qualification.each do |u|%>
      <tr>
        <td><%=u.teacher_id%></td>
        <td><%=u.degree%></td>
        <td><%=u.board_id%></td>
        <td><%=u.university_id%></td>
        <td><%=u.mode%></td>
        <td><%=u.year_of_passing%></td>
        <td><%=u.marks_obtained%></td>
        <td><%=u.percentage%></td>
        <td><%=link_to("edit",:controller=>"qualifications",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"qualifications",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
