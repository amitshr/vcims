
  <div class="well">
  <h2>Allocations</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Teacher Email</th>
        <th>Batch</th>
        <th>Detail</th>
        <th>Address</th>
        <th>Qualification</th>
        <th>Experience</th>
         <th>Edit</th>
          <th>Delete</th>
        <th>Profile</th>
      </tr>
    </thead>
    <tbody>
    <%if @teacher_status%>
    <% @teacher_status.each do |u|%>
      <tr>
        <td><%=User.find(u.user_id).email%></td>
        <td><%=Batch.find(u.batch_id).batch_name%></td>
        <td><%=link_to("enter detail",:controller=>"teachers",:action=>"new",:id=>u.user_id)%></td>
        <td><%=link_to("enter address",:controller=>"addresses",:action=>"new",:id=>u.user_id)%></td>
        <td><%=link_to("enter qualification",:controller=>"qualifications",:action=>"new",:id=>u.user_id)%></td>
       <td><%=link_to("enter experience",:controller=>"experiences",:action=>"new",:id=>u.user_id)%></td>
       
        <td><%=link_to("edit",:controller=>"teacher_statuses",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"teacher_statuses",:action=>"delete",:id=>u.user_id)%></td>
        <td><%=link_to("teacher profile",:controller=>"teachers",:action=>"teacher",:id=>u.user_id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
