
  <div class="well">
  <h2>Experiences</h2>
  <p>Experiences:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Teacher id</th>
         <th>Organization</th>
          <th>Experience From</th>
           <th>To </th>
            <th>Status</th>
          <th>Edit</th>
          <th>Delete</th>
        
      </tr>
    </thead>
    <tbody>
    <%if @experience%>
    <% @experience.each do |u|%>
      <tr>
        <td><%=u.teacher_id%></td>
         <td><%=u.organization_name%></td>
          <td><%=u.from%></td>
           <td><%=u.to%></td>
            <td><%=u.status%></td>
        
        <td><%=link_to("edit",:controller=>"experiences",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"experiences",:action=>"delete",:id=>u.id)%></td>
        
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
