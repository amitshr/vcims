
  <div class="well">
  <h2>Books</h2>
  <p>This table conatinesBook Details:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Title</th>
        <th>Summary</th>
        <th>Author</th>
        <th>Subject</th>
        <th>Class</th>
        <th>Board</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @book%>
    <% @book.each do |u|%>
      <tr>
        <td><%=u.title%></td>
        <td><%=u.summary%></td>
        <td><%=u.author%></td>
        <td><%=u.subject_id%></td>
        <td><%=u.klass_id%></td>
        <td><%=u.board_id%></td>
        <td><%=link_to("edit",:controller=>"books",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"books",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>