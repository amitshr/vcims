
  <div class="well">
  <h2>Test</h2>
  <p>Test Details:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
       <th>Full Name</th>  
        <th>Short Name</th>     
        <th>State</th>
           <th>Edit</th>
           <th>Delete</th>
           
      </tr>
    </thead>
    <tbody>
    <%if @university%>
    <% @university.each do |u|%>
      <tr>
        <td><%=u.full_name%></td>
        <td><%=u.short_name%></td>
        <td><%=u.state%></td>
        <td><%=link_to("edit",:controller=>"universities",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"universities",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
