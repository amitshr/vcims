
  <div class="well">
  <h2>Comments</h2>
  <p>List of All Comments:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
      <th>Post</th>
      <th>content </th>
      <th>Comment By</th>
        <th>status</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @comment%>
    <% @comment.each do |u|%>
      <tr>
         <td><%=u.post_id%></td>
        <td><%=u.content%></td>
         <td><%=u.user_id%></td>
         <td><%=u.status%></td>
        <td><%=link_to("edit",:controller=>"comments",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"comments",:action=>"delete",:id=>u.id)%></td>
      
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  	<%if @error%>
		<div class="alert alert-warning">
    		<%= render"shared/error"%>
		</div>
   <%end%>

  </div>
