
  <div class="well">
  <h2>Subject</h2>
  <p>This table conatines the Subject details:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Title</th>
         <th>Edit</th>
          <th>Delete</th>
        
      </tr>
    </thead>
    <tbody>
    <%if @subject%>
    <% @subject.each do |u|%>
      <tr>
        <td><%=u.title%></td>
        <td><%=link_to("edit",:controller=>"subjects",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"subjects",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>