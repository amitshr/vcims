
  <div class="well">
  <h2>Colleges</h2>
  <p>List of All Colleges:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
      <th>College Name</th>
      <th>City</th>
      <th>State</th>
      <th>university</th>
      <th>Type</th>
      <th>Mode</th>
      <th>Contact Number</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @college%>
    <% @college.each do |u|%>
      <tr>
         <td><%=u.college_name%></td>
        
        <td><%=u.city%></td>
         <td><%=u.state%></td>
         <td><%=University.find(u.university_id).short_name%></td>
         <td><%=u.college_type%></td>
         <td><%=u.education_mode%></td>
         <td><%=u.contact_number%></td>   
        <td><%=link_to("edit",:controller=>"colleges",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"colleges",:action=>"delete",:id=>u.id)%></td>
      
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  	<%if @error%>
		<div class="alert alert-warning">
    		<%= render"shared/error"%>
		</div>
   <%end%>

  </div>
