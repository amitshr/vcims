
  <div class="well">
  <h2>Batches</h2>
  <p>This table conatines the Batch detail For Student:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>State</th>
        <th>short name</th>
        <th>full name</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @board%>
    <% @board.each do |u|%>
      <tr>
        <td><%=u.state%></td>
        <td><%=u.short_name%></td>
        <td><%=u.full_name%></td>
        <td><%=link_to("edit",:controller=>"boards",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"boards",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
