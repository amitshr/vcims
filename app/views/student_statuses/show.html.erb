<div class="well">
  <h2>Allocations</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Batch</th>
        <th>Email</th>
        <th>Detail</th>
        <th>Address</th>
         <th>Edit</th>
          <th>Delete</th>
          <th>Profile</th>
      </tr>
    </thead>
    <tbody>
    <%if @student_status%>
    <% @student_status.each do |u|%>
      <tr>
        <td><%=Batch.find(u.batch_id).batch_name%></td>
        <td><%=User.find(u.user_id).email%></td>
        <td><%=link_to("enter detail",:controller=>"students",:action=>"new",:id=>u.user_id)%></td>
        <td><%=link_to("enter address",:controller=>"addresses",:action=>"new",:id=>u.user_id)%></td>
        <td><%=link_to("edit",:controller=>"student_statuses",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"student_statuses",:action=>"delete",:id=>u.id)%></td>
        <td><%=link_to("student profile",:controller=>"students",:action=>"student",:id=>u.user_id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>