

  <div class="well">
  <h2>Centers</h2>
  <p>List of All Ceneters:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
      <th>Center Email</th>
      <th>Center Name </th>
      <th>Incharge Name</th>
        <th>Contact Number</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @center%>
    <% @center.each do |u|%>
      <tr>
         <td><%=User.find(u.user_id).email%></td>
        
        <td><%=u.center_name%></td>
         <td><%=u.center_incharge%></td>
         <td><%=u.mobile_number%></td>
        
        <td><%=link_to("edit",:controller=>"centers",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"centers",:action=>"delete",:id=>u.id)%></td>
      
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  	<%if @error%>
		<div class="alert alert-warning">
    		<%= render"shared/error"%>
		</div>
   <%end%>

  </div>
