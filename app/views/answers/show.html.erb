

  <div class="well">
  <h2>Answers</h2>
  <p>This table conatines the address detail about user:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
   
        <th>Question</th>
         <th>Answer</th>
          <th>Edit</th>
           <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @answer%>

    <% @answer.each do |u|%>
      <tr>
          <td><%=Question.find(u.question_id).content%></td>
        <td><%=u.content%></td>
        <td><%=link_to("edit",:controller=>"answers",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"answers",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
