
  <div class="well">
  <h2>Test</h2>
  <p>Test Details:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
       <th>Course</th>  
        <th>Test Code</th>     
        <th>Note</th>
        <th>Test Type</th>       
         <th>Content</th>
          <th>Marks</th>
           <th>Time Duration</th>
           <th>Edit</th>
           <th>Delete</th>
            <th>printable Format</th>
           
      </tr>
    </thead>
    <tbody>
    <%if @test%>
    <% @test.each do |u|%>
      <tr>
       
        <td><%=Course.find(u.course_id).course_name%></td>
        <td><%=u.test_code%></td>
        <td><%=u.note%></td>
        <td><%=u.test_type%></td>
        <td><%=u.content%></td>
        <td><%=u.marks%></td>
        <td><%=u.time_duration%></td>
        <td><%=link_to("edit",:controller=>"tests",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"tests",:action=>"delete",:id=>u.id)%></td>
        <td><%=link_to("Printable format",:controller=>"tests",:action=>"profile",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
     
