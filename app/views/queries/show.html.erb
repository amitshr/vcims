
  <div class="well">
  <h2>View All Queries</h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Email</th>
         <th>contact</th>
          <th>Address</th>
          <th>Question</th>
          <th>Reply</th>
          <th>Status</th> 
          <th>Edit</th>
          <th>Delete</th>          
      </tr>
    </thead>
    <tbody>
    <%if @query%>
    <% @query.each do |u|%>
      <tr>
        <td><%=u.email%></td>
        <td><%=u.mobile_number%></td>
        <td><%=u.line1+u.line2+u.city+u.state%></td>
        <td><%=u.question%></td>
        <td><%=u.reply%></td>
        <td><%=u.status%></td>
        <td><%=link_to("edit",:controller=>"queries",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"queries",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
