
  <div class="well">
  <h2>Newses  </h2>
  <p>Links to destroy or edit details for user are given at end of each row :</p><p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>user</th>
         <th>content</th>
          <th>image</th>
           
          <th>Edit</th>
          <th>Delete</th>          
      </tr>
    </thead>
    <tbody>
    <%if @news%>
    <% @news.each do |u|%>
      <tr>
        <td><%=User.find(u.user_id).email%></td>
        <td><%=u.content%></td>
        <td><%=u.image_id%></td>
        <td><%=link_to("edit",:controller=>"newses",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"newses",:action=>"delete",:id=>u.id)%></td>
        <td><%=link_to("profile",:controller=>"newses",:action=>"profile",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>