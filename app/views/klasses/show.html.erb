
  <div class="well">
  <h2>Classes</h2>
  <p>List of All Classes:</p>
  <p>Links to destroy or edit details for user are given at end of each row :</p>            
  <p>Use delete button with caution once triggered can not recover the details again:</p>
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Title</th>
         <th>Edit</th>
          <th>Delete</th>
      </tr>
    </thead>
    <tbody>
    <%if @klass%>
    <% @klass.each do |u|%>
      <tr>
        <td><%=u.title%></td>
        <td><%=link_to("edit",:controller=>"klasses",:action=>"edit",:id=>u.id)%></td>
        <td><%=link_to("delete",:controller=>"klasses",:action=>"delete",:id=>u.id)%></td>
      </tr>
     <%end%>
     <%end%>       
    </tbody>
  </table>
  </div>
